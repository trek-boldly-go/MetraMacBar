# Swift build artifacts
.build/

# Built app bundle (binary — rebuild with `make run`)
MetraTracker.app/

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Xcode (if opened with Xcode)
xcuserdata/
DerivedData/
*.xcworkspace/xcuserdata/

# Claude Code local settings (user-specific permissions)
.claude/
